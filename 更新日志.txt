22:10 2023/10/21：
1、增加离散粒子群函数（DPSO），个体的维度为r（校验块数量），每个维度的取值为 1:S 的整数，和GA算法一样。与BPSO的差异体现在粒子的更新过程，需要保证粒子的没有重复维度。
DPSO相对于BPSO的优点：
》粒子更新后仍然满足有效性，而BPSO更新后往往 1 的数量不等于 r ，就会导致出现很多无效的粒子，从而使得效果变差；
》卫星参数和种群都改用结构体编写，使结果更加统一便于观察。
2、增加绘图函数plot_res()，用于比较两种算法的结果。